#include <iostream>
using namespace std;
int main(){
    int n,k=0;
    cin>>n;
    int m=n;
    while (n>0){
        k=k*10+n%10;
        n/=10;
    }
    cout<<"Ters eded:"<<k<<endl;
    if (k==m)cout<<"Eded palindromdur.";
    else cout<<"Eded palindrom deyil.";
    return 0;
}