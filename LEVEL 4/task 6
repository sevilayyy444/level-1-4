#include <iostream>
using namespace std;
int main() {
    int N, x;
    cin>>N>>x;
    int mn=x, mx=x;
    long long a=x;
    for (int i=1;i<N;i=i+1){
        cin>>x;
        if (x<mn) mn = x;
        if (x>mx) mx = x;
        a=a+x;
    }
    cout<<mn<<" "<<mx<<" "<<(double)a/N;
    return 0;
}